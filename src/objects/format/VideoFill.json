{
    "name": "Format\\VideoFill",
    "description": "The object for adding a video background fill to a component.",
    "version": "1.7+",
    "from_class": null,
    "extends": "Format\\Fill",
    "url": "https:\/\/developer.apple.com\/documentation\/apple_news\/videofill",
    "properties": [
        {
            "name": "URL",
            "type": "uri",
            "required": true,
            "description": "The URL of a video file that can be played using AV Player. HTTP Live Streaming (HLS) is highly recommended (.M3U8). For more information on HLS, refer to the iOS developer documentation on HTTP Live Streaming, especially the following sections of the HTTP Live Streaming Overview:",
            "version": "1.0"
        },
        {
            "name": "fillMode",
            "type": "enum:string",
            "required": false,
            "description": "Indicates how the video fill should be displayed. Valid values:",
            "version": "1.0",
            "default": "cover",
            "enum_values": [
                "fit",
                "cover"
            ]
        },
        {
            "name": "horizontalAlignment",
            "type": "enum:string",
            "required": false,
            "description": "Sets the horizontal alignment of the video fill within its component.",
            "version": "1.0",
            "default": "center",
            "enum_values": [
                "left",
                "center",
                "right"
            ]
        },
        {
            "name": "loop",
            "type": "boolean",
            "required": false,
            "description": "When true, it specifies that the video will start over again when it reaches the end.",
            "version": "1.1",
            "default": "true"
        },
        {
            "name": "stillURL",
            "type": "string",
            "required": true,
            "description": "The URL of the image file to use as a still image when the video is not playing.",
            "version": "1.0"
        },
        {
            "name": "type",
            "type": "string",
            "required": true,
            "description": "Describes the type of fill. Must be video for a video fill.",
            "version": "1.0"
        },
        {
            "name": "verticalAlignment",
            "type": "enum:string",
            "required": false,
            "description": "Sets the vertical alignment of the video fill within its component. Valid values:",
            "version": "1.0",
            "default": "center",
            "enum_values": [
                "top",
                "center",
                "bottom"
            ]
        }
    ]
}